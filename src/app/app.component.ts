import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //es el nombre que tendrá el componente
  templateUrl: './app.component.html', // esta es la ubicacion del componente
  styleUrls: ['./app.component.css'] // esta es la direccion de la estilizacion del componente
})
export class AppComponent {
  
}
